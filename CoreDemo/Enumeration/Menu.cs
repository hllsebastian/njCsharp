// Utilizando el "enum" se podra asignar a una lista, un orden numerico, si no se define el valor como
// como en este caso, por defecto la primera posicion es cero  
  
  
  namespace CoreDemo.Enumeration
  {
      public enum Menu
      {    
            ninguno,    
            año = 1,
            enterO = 2,
            dateNow = 3,
            contadorFor = 4,
            contadorWhile = 5,
            vectorEnteros,
            Diccionario        
      }
  }
  
  
    